# Week 2 성능 측정 계획 (p95/TPS)

## 1) 목표
- 이벤트 기반 주문/정산 POC의 기본 처리 성능을 수치로 확인한다.
- 최소 1회 개선 전/후 비교가 가능하도록 동일한 측정 절차를 정의한다.

## 2) 측정 대상
- API 발행 경로
  - `POST /api/v1/events/orders`
- Kafka 처리 경로
  - Producer 발행 성공 로그 (`kafka.publish.success`)
  - Consumer 소비 성공 로그 (`kafka.consume.success`)

## 3) 핵심 지표
- API p95 latency (ms)
- API TPS (requests/sec)
- Error rate (%)
- Kafka consume lag (가능 시)
- E2E 처리 시간(ms)
  - 기준: 발행 시각 ~ 소비 성공 로그 시각

## 4) 목표 기준(초안)
- API p95 <= 200ms (로컬 개발환경 기준)
- TPS >= 100 req/s (단일 인스턴스 기준)
- Error rate <= 1%
- 강제 실패(`forceFail=true`) 제외 정상 이벤트는 소비 누락 0건

## 5) 시나리오

### 시나리오 A: 정상 이벤트 단건/저부하
- 목적: 기준선(Baseline) 확보
- 조건: 1분, 동시성 10
- 요청 본문: `forceFail=false`

### 시나리오 B: 정상 이벤트 중부하
- 목적: p95/TPS 구간 확인
- 조건: 3분, 동시성 50
- 요청 본문: `forceFail=false`

### 시나리오 C: 실패 이벤트 혼합
- 목적: 실패 로그 처리 시 안정성 확인
- 조건: 3분, 동시성 30, 실패 요청 5%
- 요청 본문: `forceFail=true`를 5% 비율로 섞음

## 6) 측정 절차
1. 사전 준비
   - `docker compose up -d`
   - 앱 실행(`bootRun`) 후 헬스 확인
2. 워밍업
   - 1분간 저부하 트래픽으로 JIT/커넥션 안정화
3. 본 측정
   - 시나리오 A/B/C 순서로 진행
   - 각 시나리오마다 API 응답 통계와 앱 로그 수집
4. 결과 집계
   - p95/TPS/Error rate 표 작성
   - publish/consume 성공 건수 일치 여부 확인
5. 개선안 적용 후 재측정
   - 동일 시나리오로 1회 이상 반복 측정

## 7) 도구
- 부하 도구(택1)
  - `k6` (권장)
  - 또는 `hey`, `wrk`
- 로그 분석
  - 애플리케이션 콘솔 로그
  - 필요 시 Kafka UI로 토픽 상태 보조 확인

## 8) 결과 기록 템플릿

| Scenario | Avg(ms) | p95(ms) | TPS | Error Rate | Publish Success | Consume Success | Notes |
|---|---:|---:|---:|---:|---:|---:|---|
| A | - | - | - | - | - | - | - |
| B | - | - | - | - | - | - | - |
| C | - | - | - | - | - | - | - |

## 8-1) 실측 결과 (2026-02-27)

| Scenario | Avg(ms) | p95(ms) | TPS | Error Rate | Publish Success | Consume Success | Notes |
|---|---:|---:|---:|---:|---:|---:|---|
| A | 1.15 | 1.52 | 120 | 0.00% | 요청 수와 동일 수준 확인 | 요청 수와 동일 수준 확인 | 60초, 동시성 10 |
| B | 1.16 | 1.33 | 300 | 0.00% | 요청 수와 동일 수준 확인 | 요청 수와 동일 수준 확인 | 180초, 동시성 50 |
| C | 1.22 | 1.45 | 200 | 0.00% | 요청 수와 동일 수준 확인 | 정상 소비 로그 지속 확인 | 180초, 동시성 30, 실패 이벤트 5% 혼합 |

측정 원본 파일
- `performance-week2-result.json`

목표 기준 비교
- API p95 <= 200ms: **충족** (A/B/C 모두 2ms 이내)
- TPS >= 100 req/s: **충족** (A 120, B 300, C 200)
- Error rate <= 1%: **충족** (A/B/C 모두 0.00%)

## 9) 리스크 및 대응
- 로컬 머신 상태 변동으로 결과 편차 발생 가능
  - 대응: 각 시나리오 2~3회 반복, 중앙값/범위 함께 기록
- 로그 기반 E2E 시간 추출의 수작업 비용
  - 대응: traceId 기준 샘플 20건만 우선 수집
- 과도한 무제한 전송 시 애플리케이션 소켓/스레드 적체 가능
  - 대응: 고정 요청 수 + worker pacing + 시나리오 간 cooldown 적용

## 10) 완료 기준
- p95/TPS 기준이 포함된 계획 문서 1개 작성
- 시나리오 3개 이상, 지표/도구/절차/목표값 포함
- 다음 주 실행 시 그대로 따라할 수 있는 수준으로 문서화
